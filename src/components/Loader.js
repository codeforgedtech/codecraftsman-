import React from 'react';
import '../styles/Loader.css'; // Lägg till din loader-stil här

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
  </div>
);

export default Loader;